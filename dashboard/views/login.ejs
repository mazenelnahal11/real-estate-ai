<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        'off-white': '#F5F5F0',
                        zinc: {
                            50: '#FAFAFA',
                            100: '#F5F5F5',
                            850: '#1A1A1A',
                            900: '#111111',
                            950: '#000000', // Deep Black
                        },
                        emerald: tailwind.colors.zinc, // Map emerald to zinc to neutralize
                    },
                    animation: {
                        'fade-in-up': 'fadeInUp 0.6s ease-out forwards',
                        'float': 'float 6s ease-in-out infinite',
                    },
                    keyframes: {
                        fadeInUp: {
                            '0%': { opacity: '0', transform: 'translateY(20px)' },
                            '100%': { opacity: '1', transform: 'translateY(0)' },
                        },
                        float: {
                            '0%, 100%': { transform: 'translateY(0)' },
                            '50%': { transform: 'translateY(-10px)' },
                        }
                    }
                }
            }
        }
    </script>
    <style>
        html {
            font-size: 20px;
        }
    </style>
    <script>
        if (localStorage.getItem('theme') === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
        } else {
            document.documentElement.classList.remove('dark');
        }
    </script>
</head>

<body
    class="bg-off-white dark:bg-zinc-950 text-zinc-900 dark:text-zinc-100 h-screen flex items-center justify-center relative overflow-hidden transition-colors duration-300">

    <!-- Ambient Background -->
    <div class="absolute inset-0 pointer-events-none">
        <div
            class="absolute top-1/4 left-1/4 w-96 h-96 bg-zinc-400/10 dark:bg-zinc-800/10 rounded-full blur-3xl opacity-30 animate-float">
        </div>
        <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-zinc-300/10 dark:bg-zinc-700/10 rounded-full blur-3xl opacity-30 animate-float"
            style="animation-delay: -2s"></div>
    </div>

    <!-- Login Card -->
    <div
        class="w-full max-w-md p-8 space-y-8 bg-white/80 dark:bg-zinc-900/80 backdrop-blur-xl rounded-3xl border border-zinc-200 dark:border-zinc-800 shadow-2xl relative z-10 animate-fade-in-up">
        <div class="text-center space-y-2">
            <h1 class="text-3xl font-bold tracking-tight text-zinc-900 dark:text-white">
                Admin Access</h1>
            <p class="text-zinc-500 dark:text-zinc-400 text-sm">Please verify your credentials</p>
        </div>

        <% if (locals.error) { %>
            <div
                class="p-4 text-sm text-red-500 bg-red-500/10 border border-red-500/20 rounded-xl text-center animate-fade-in-up font-medium">
                <%= error %>
            </div>
            <% } %>

                <form action="/login" method="POST" class="space-y-6">
                    <div class="space-y-4">
                        <div>
                            <label
                                class="block text-xs font-semibold text-zinc-500 dark:text-zinc-400 uppercase tracking-wider mb-2">Username</label>
                            <input type="text" name="username" required
                                class="w-full px-4 py-3 bg-zinc-50 dark:bg-zinc-950/50 border border-zinc-200 dark:border-zinc-800 rounded-xl focus:ring-2 focus:ring-zinc-500/50 focus:border-zinc-500 outline-none transition-all placeholder-zinc-400 dark:placeholder-zinc-600 text-zinc-900 dark:text-zinc-200 shadow-xl">
                        </div>
                        <div>
                            <label
                                class="block text-xs font-semibold text-zinc-500 dark:text-zinc-400 uppercase tracking-wider mb-2">Password</label>
                            <input type="password" name="password" required
                                class="w-full px-4 py-3 bg-zinc-50 dark:bg-zinc-950/50 border border-zinc-200 dark:border-zinc-800 rounded-xl focus:ring-2 focus:ring-zinc-500/50 focus:border-zinc-500 outline-none transition-all placeholder-zinc-400 dark:placeholder-zinc-600 text-zinc-900 dark:text-zinc-200 shadow-xl">
                        </div>
                    </div>

                    <button type="submit"
                        class="w-full py-3 px-4 bg-zinc-900 dark:bg-white hover:bg-zinc-800 dark:hover:bg-zinc-200 text-white dark:text-zinc-900 font-bold rounded-xl transition-all shadow-xl hover:shadow-2xl active:scale-95 transform duration-200">
                        Sign In
                    </button>
                </form>

                <div class="text-center">
                    <p class="text-[10px] text-zinc-400 dark:text-zinc-600">Secure System • Authorized Personnel Only
                    </p>
                </div>
    </div>
</body>

</html>